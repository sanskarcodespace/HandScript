/**
 * Express Route Map: adminRoutes
 * Binds endpoints to corresponding controller methods.
 */
import { Router } from 'express';

const router = Router();
// TODO: Define routes (e.g., router.get('/', controller))

export default router;
